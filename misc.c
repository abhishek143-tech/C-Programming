#include <stdio.h>

int main()
{
    // int num1;
    // char a;
    // float age;
    // printf("%d",sizeof(age));

    // int a;
    // printf("%p \n",&a);

    // int a;
    // int *ptr = &a;

    int num1 = 1;
    num1 > 5 ? printf("num1 is greater than 5 \n") : printf("num1 is less than 5 \n");
    //if above condition is true then it executes first statement and if it is false then second statement
} 